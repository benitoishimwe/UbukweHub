'use strict';

/**
 * DEPRECATED — this file is intentionally empty.
 *
 * Routes previously defined here have been moved to super-admin.routes.js,
 * which the auto-loader mounts at the correct path  /api/super-admin.
 *
 * This file is kept so that git history is preserved.
 * The auto-loader will mount an empty router at /api/superAdmin (harmless).
 */

const { Router } = require('express');

module.exports = Router();
